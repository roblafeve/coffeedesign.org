.button
  @extend $skin-text_box
  display inline-block
  color white
  font-weight 700
  text-transform uppercase
  background violet
  box-shadow none
  &.sub-white
    @extend $skin-text_box.sub-white
    box-shadow inset 0 0 0 2px
    background transparent