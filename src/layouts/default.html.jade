!!!
html(lang="en")
  include partial/head
  body
    .background
    include partial/header
    main.layout-site_content
      != content
    include partial/footer
    != getBlock('scripts').add([site.url + "/vendor/jquery.js", site.url + "/javascript/functions.js"]).toHTML()